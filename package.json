{
  "name": "js-koans",
  "version": "0.1.0",
  "description": "将目录通过 `git clone` 或者打包下载到你的机器上。",
  "main": "index.js",
  "scripts": {
    "start": "./node_modules/browser-sync/bin/browser-sync.js start --server --files \"exercises/*.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.13.0"
  }
}
